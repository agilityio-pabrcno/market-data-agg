# PostgreSQL
POSTGRES_USER=marketdata
POSTGRES_PASSWORD=marketdata
POSTGRES_DB=marketdata

# Database URL (used by the app - defaults work with compose)
# DATABASE_URL=postgresql://marketdata:marketdata@postgres:5432/marketdata
